import {apiRequest} from './apiClient';
export const requirementRepository = { list: () => apiRequest<unknown>('/api/requirements'), detail: (id:string) => apiRequest<Record<string,unknown>>(`/api/requirements/${encodeURIComponent(id)}`), departments: () => apiRequest<unknown[]>('/api/requirements/departments'), employees: () => apiRequest<Array<Record<string, unknown>>>('/api/auth/dev-accounts'), retryWorkItem: (id:string) => apiRequest<Record<string,unknown>>(`/api/requirements/work-items/${encodeURIComponent(id)}/retry`,{method:'POST'}), create: (body:Record<string,unknown>) => apiRequest('/api/requirements',{method:'POST',body:JSON.stringify(body)}), update: (id:string, body:Record<string,unknown>) => apiRequest(`/api/requirements/${encodeURIComponent(id)}`,{method:'PUT',body:JSON.stringify(body)}), events: (id:string) => apiRequest<Array<Record<string,unknown>>>(`/api/requirements/${encodeURIComponent(id)}/events`), comment: (id:string, body:Record<string,unknown>) => apiRequest(`/api/requirements/${encodeURIComponent(id)}/comments`,{method:'POST',body:JSON.stringify(body)}), transition: (id:string, body:Record<string,unknown>) => apiRequest(`/api/requirements/${encodeURIComponent(id)}/transition`,{method:'POST',body:JSON.stringify(body)}), createWorkItem: (id:string, body:Record<string,unknown>) => apiRequest(`/api/requirements/${encodeURIComponent(id)}/work-items`,{method:'POST',body:JSON.stringify(body)}) };
