<script setup lang="ts">import DetailDrawer from './DetailDrawer.vue';import DetailFields from './DetailFields.vue';type Field={key:string;label:string};defineProps<{open:boolean;title:string;record?:Record<string,unknown>|null;fields:Field[];loading?:boolean;error?:string}>();const emit=defineEmits<{close:[]}>();</script>
<template><DetailDrawer :open="open" :title="title" @close="emit('close')"><p v-if="loading" class="state">正在加载详情…</p><p v-else-if="error" class="state error">{{error}}</p><DetailFields v-else-if="record" :record="record" :fields="fields"/><slot v-if="!loading && !error"/></DetailDrawer></template>
