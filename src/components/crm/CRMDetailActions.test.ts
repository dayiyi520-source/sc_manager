import { describe, expect, it } from 'vitest';

describe('CRMDetailActions contract', () => {
  it('uses a shared action container for detail pages', () => {
    expect('flex flex-wrap items-center justify-end gap-2').toContain('flex-wrap');
  });
});
