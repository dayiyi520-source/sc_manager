# 目标与绩效本地验收

日期：2026-09-14。需求：7.1。当前有效范围以本记录及 PRD 7.1 为准。

## 当前界面

- 保留原目标卡片、分类、页内添加目标，以及原周/月复盘表单与复盘列表。
- 目标只增加直属上级同月已确认 OKR 的选择；复盘只增加本人本期已完成任务或工单的多选关联。
- 不显示上一轮扩展工作区、独立任务关联管理、逐项产出及延期影响编辑。
- 按钮、输入、数字、选择、日期、单选、进度、卡片、空状态及复盘页签采用 Ant Design；复用 OkrProvider 适配原主题。
- 添加目标在加载中及接口失败时仍能展开，仅提交操作等待数据恢复。失败保留输入并提供重试。

## 访问与服务

- 验收地址：http://127.0.0.1:3011/app/wb_okr_perf 。
- 使用已有开发账号 tech；登录页拉下台灯拉绳，选择 tech 并进入工作空间。
- 独立后端为本机 8081，独立 MySQL 为本机 3307，数据库为 manage_admin_okr_acceptance。
- 本轮发现独立数据库进程退出导致加载失败，已重新独立启动并登记 okr-db；后端已更新 runtime 验收 JAR，并设置本地连接等待为 3 秒。
- 服务状态检查：python .enterprise-app-factory/bin/local_services.py --project D:/project/manage_admin-okr status --json 。
- 未修改共享服务、生产数据库或生产部署。

## 已执行验证

- bun run lint：TypeScript 检查通过。
- bun run build：构建通过，存在既有大分包提示。
- 前端关联筛选、工作归集和会话回归共 17 项通过。
- OkrPolicyTest 与 OkrIntegrationTest 共 12 项通过；涵盖实际工作关联持久化、无需关联 KR 或逐项产出、未完成工作拒绝、原表单字段持久化及原有权限/状态校验。测试使用独立数据库，事务回滚。
- scripts/verify-okr-original-browser.cjs 在 1920x1080 使用真实后端验证加载、页内添加、上级选择、目标提交与刷新、复盘提交与刷新、空候选、服务失败重试、输入保留、加载时展开、暗色适配和原生控件扫描。
- 浏览器脚本的 503 与延迟响应仅模拟网络状态；业务创建保存使用真实后端，会生成带“验收”标题的本地目标和复盘。
- PRD 校验通过。

## 周期筛选与控件调整

- 主导航、目标分类和复盘分类均为 Ant Design 基本 Tabs；排除旧全局 tab 蓝底白字覆盖，选中仅蓝色文字与下划线。
- 周期使用多选 Cascader，当前月进行中；本人整月复盘最终评分已确认的历史月已归档，其余历史月已结束。零分计为有效确认，周复盘不归档整月。
- 单选回填“周期：年月”，多选回填“周期：xx个周期”。周期候选包括最近十二个月及可见记录中的更早月份。
- 添加目标移除月份选择，标题旁显示当前月份；后端禁止创建或提交历史月份目标。真实接口验证历史月请求返回 400。
- 原控件为 Ant Design，但全局原生 input 的强制背景、圆角与焦点样式曾覆盖内部输入框。本轮仅将本模块排除这些覆盖，未新增控件包裹层。
- 类型检查、构建、周期分组测试和 12 项相关后端测试通过；1920x1080 浏览器覆盖 Tabs 无胶囊、周期多选回填、当前月新增、内部输入框无额外边框及原提交刷新回归。

## 已知限制

- 本期已完成工作暂按完成状态及最后更新时间筛选，既有工作表没有独立完成时间；完成后再编辑可能影响周期归属，尚不能精确还原历史完成时间。
- 当前验收账号没有本期已完成候选，浏览器验证空状态；非空关联及未完成拒绝由数据库集成测试验证。
- 参与人保留原输入方式，本轮未新增消息发送。
- 本地 MySQL 版本为 8.4，未验证生产 MySQL 9.3；未部署线上。
